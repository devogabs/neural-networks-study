# Neural Network Study

A repository dedicated to exploring neural network architectures and implementation in Python.

## 🚀 Getting Started
1. Clone the repo:
   `git clone git@github.com:devogabs/neural-networks-study.git`
2. Install dependencies:
   `pip install -r requirements.txt`

## 🧠 Topics Covered
- Basic Perceptrons
- Backpropagation

